name: Local CD
on:
  push:
    branches: [ main ]
jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Pull and restart app
        run: |
          cd /disbot/disbot/
          git pull origin main
          systemctl restart disbot.service
